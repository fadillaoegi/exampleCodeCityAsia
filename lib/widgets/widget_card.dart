import 'package:flutter/material.dart';


class CardDefault extends StatelessWidget {
  const CardDefault({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 40.0,
      width: ,
    ) ;
  }
}